apiVersion: kubeprober.erda.cloud/v1alpha1
kind: Probe
metadata:
  name: probe-link-test1
  namespace: kubeprober
spec:
  # Add fields here
  probeList:
    # Probe item1
    - name: probe-item-test1-demo-error
      spec:
        containers:
          - name: demo-error
            image: kubeprober/probe-test-demo-error:v0.0.1
        restartPolicy: Never
